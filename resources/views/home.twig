{% extends 'templates/app.twig'%}


{% block content %}
   <div class="row">
     {% for index, product in products %}
        <div class="col-md-4 col-sm-6">

            {% include 'products/partials/items.twig' with {item: product} %}

            {% if(index+1)%3 == 0 %}
                </div>
                <div class="row">
            {% endif%}

        </div>

     {% endfor %}

   </div>

{% endblock %}
